<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/MyAppThemeSwitch"
        tools:targetApi="31">
        <activity
            android:name=".UserService.EditProfileActivity"
            android:exported="false" >
        </activity>
        <activity
            android:name=".UserService.ProfileActivity"
            android:exported="false"/>
        <activity
            android:name=".UserService.ChangePasswordActivity"
            android:exported="false" />
        <activity
            android:name=".UserService.SignupActivity"
            android:exported="false" >
        </activity>
        <activity
            android:name=".UserService.LoginActivity"
            android:exported="true" >

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true" >
        </activity>
    </application>

</manifest>